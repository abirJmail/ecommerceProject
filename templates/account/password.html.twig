{% extends 'base.html.twig' %}

{% block title %}Modifier mon mot de passe - La Boutique Française{% endblock %}

{% block content %}
<div class="account">
<a class="btn btn-sm headerBtnTitle btnDeco " href="{{ path('app_logout') }}">Déconnexion</a>
    <h1>Modifier mon mot de passe</h1>
    <a href="{{ path('app_account') }}">Retourner à mon compte</a><br/>
    <hr>
    {% if notification %}
        <div class="alert alert-info">{{ notification }}</div>
    {% endif %}
    <div class="w-75">
    {{ form(form) }}
    </div>
    <hr>
    </div>
{% endblock %}